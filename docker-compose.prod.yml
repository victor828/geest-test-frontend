services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      target: prod
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-https://geest.veom.lat}
    image: geest-test-frontend:prod
    ports:
      - "${PROD_PORT:-8080}:${PORT:-8080}"
    environment:
      PORT: ${PORT:-8080}
    restart: unless-stopped
